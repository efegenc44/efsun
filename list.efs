module Main

let main = \a
    map (\x "mapped") (List.Cons "AA" (List.Cons "BB" List.Empty))

let map = \f \l match l
    | List.Empty       = List.Empty
    | List.Cons ~x ~xs = List.Cons (f x) (map f xs)

structure List[a]
    | Empty
    | Cons a List[a]
