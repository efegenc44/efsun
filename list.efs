module Main

let main = \a
    map

let map = \f \l match l
    | List.Empty       = List.Empty
    | List.Cons ~x ~xs = List.Cons (f x) (map f xs)

structure List[a]
    | Empty
    | Cons a List[a]
