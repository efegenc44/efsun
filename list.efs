module Main

let main = \a
    map (\x "mapped") (Cons "AA" (Cons "BB" Empty))

let map = \f \l match l
    | Empty       = Empty
    | Cons ~x ~xs = Cons (f x) (map f xs)

structure List[a]
    | Empty
    | Cons a List[a]

import Main.List in