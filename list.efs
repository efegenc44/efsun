module Main

let main = \a
    map (\x "mapped") (Cons "AA" (Cons "BB" nil))

let map = \f \l match l
    | nil         = nil
    | Cons ~x ~xs = Cons (f x) (map f xs)

structure List[a]
    | Empty
    | Cons a List[a]

import Main.List (Empty as nil Cons)